# tuple cannot be changed, but the elements in the tuple can be changed
# tuple is immutable cannot be remoced, added or changed
# we can only count the elements in the tuple
# we can only find the index of the element in the tuple
numbers = (1,2,3,4,5)
print(numbers)
